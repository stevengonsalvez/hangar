{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: core IPC for the shell's own commands and events.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
