# What both renderers must show for the inbox fixture: one list, checked
# against the ratatui snapshot (ainb-core/tests/parity_snapshots.rs and
# ainb-app/tests/parity.rs) and against the webview's own render
# (ainb-desktop/ui/src/parity.test.ts). A fact deleted from either renderer's
# output fails the suite, and each half has a mutation test that proves the
# list can fail: a row lost from the section is a fact lost from the screen.
#
# The fixture carries 101 rows (three named, 98 filler) and one summary past
# the 256-character cut, so the cut path is what the facts read: the row past
# the cap is not shown, the long summary ends in the marker, and the counters
# say so in the one wording both renderers use. Facts are renderer-neutral:
# what is shown, not how it is framed, so the keys and buttons are not here.
New issue: the inbox reads the daemon
New comment on issue-7
New issue: the release checklist for the desktop bundle
[cut]
1 older entry not sent
1 summary shortened
Task queued: filler-0
