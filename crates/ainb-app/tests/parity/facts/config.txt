# Expected facts of the `config` fixture, one per line, each a substring both
# renderers must print: the ratatui snapshot (`../config.snap`) and the settings
# page's lines (`ainb-desktop/ui/test/parity.test.ts`, which mounts the page). A line
# starting with `#` is a comment. A renderer that loses a row fails its
# parity test; the mutation checks beside each half prove it.
Authentication
Workspace
Docker
Agent Defaults
Editor
Plugins
MCP Pool
Appearance
General
Container Templates
Fleet
Usage
Skills
Session Reader
Presets
Daemons
Web Dashboard
ACP Adapters
Hangar Daemon
Claude Authentication
system_auth
How Claude authenticates: subscription, API key, or a cloud gateway
GitHub Auth Method
